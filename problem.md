## **問題3：複数の依存関係**

カウントと名前の2つの状態を管理し、どちらかが更新されると「状態が変更されました: [count], [name]」とコンソールに表示するコンポーネントを作成せよ。

---

### **条件**

(1) `count` と `name` を `useState` で管理すること
(2) `useEffect` の依存配列に両方の状態を設定すること
(3) TailwindCSSでボタンを緑背景・白文字にすること
